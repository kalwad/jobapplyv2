# Canonical project-memory documents are never auto-reformatted; the
# specification must not be rewritten by tooling (spec §1.4).
CLAUDE.md
docs/
MASTER_IMPLEMENTATION_SPEC(1).md

# Generated contract trees are byte-exact generator output (M01-W02).
# Reformatting them would create permanent drift against the deterministic
# emitter; integrity is enforced by `pnpm generate:contracts --check`.
packages/contracts/generated/

# Lockfiles and tool/build output
pnpm-lock.yaml
uv.lock
node_modules/
dist/
.turbo/
coverage/
.venv/
services/native-host/target/
services/native-host/Cargo.lock
test-results/
playwright-report/
blob-report/
