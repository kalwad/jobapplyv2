# OS / editor noise
.DS_Store
Thumbs.db

# Python
__pycache__/
*.py[cod]
.venv/
*.egg-info/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Node / TypeScript (used from M00-W02 onward)
node_modules/
dist/
.turbo/
coverage/

# Rust (scoped per crate; broadened if more crates arrive)
services/native-host/target/

# Playwright browser-test artifacts (traces/screenshots/videos are
# failure-only per playwright.config.ts and never committed)
test-results/
playwright-report/
blob-report/

# Local environment and secrets — never committed (spec §1.5)
.env
.env.*

# Owner's original spec upload; the canonical copy lives at
# docs/MASTER_IMPLEMENTATION_SPEC.md (spec §1.1). Ignored to prevent a
# divergent second copy from being committed.
MASTER_IMPLEMENTATION_SPEC(1).md
