{
  "name": "@japp/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "JSON Schema source of truth and generated TypeScript/Python models (spec §5.1). M01-W01 owns the hand-authored Draft 2020-12 schema source under schemas/ and the strict offline validation conventions; generation arrives in M01-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "catalog:",
    "ajv-formats": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
