{
  "name": "@japp/contracts",
  "version": "0.0.1",
  "private": true,
  "description": "JSON Schema source of truth and generated TypeScript/Python models (spec §5.1). M01-W01 owns the hand-authored Draft 2020-12 schema source under schemas/ and the strict offline validation conventions; M01-W02 owns the deterministic generator (packages/contracts/generator, scripts/generate-contracts.ts) and the generated trees under generated/.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./generated": "./generated/typescript/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "dependencies": {
    "ajv": "catalog:",
    "ajv-formats": "catalog:"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
