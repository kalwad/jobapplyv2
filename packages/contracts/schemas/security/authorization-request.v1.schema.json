{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:japp:schema:security:authorization-request:v1",
  "title": "Authorization request metadata",
  "description": "Strict closed M01-W04 request envelope containing authorization metadata only. Required capability, decision, and denial text are derived from canonical catalogs and cannot be supplied by callers. The receiver principal is trusted runtime context passed separately to authorization and is deliberately absent from this untrusted wire record.",
  "x-japp-schema-version": "1.0.0",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "request_version",
    "request_id",
    "command_id",
    "originating_principal",
    "immediate_sender",
    "target_principal",
    "authorization_profile",
    "occurred_at",
    "correlation_id",
    "payload_size_bytes"
  ],
  "properties": {
    "request_version": {
      "description": "Authorization-request record version.",
      "type": "string",
      "enum": ["AUTHORIZATION_REQUEST_V1"]
    },
    "request_id": {
      "$ref": "urn:japp:schema:common:stable-id:v1#/$defs/stableId"
    },
    "command_id": {
      "$ref": "urn:japp:schema:security:command-taxonomy:v1#/$defs/commandId"
    },
    "originating_principal": {
      "description": "Original software requester. Forwarders must preserve this value unchanged and authorization requires equality with trusted authenticated-origin context.",
      "$ref": "urn:japp:schema:security:capability-taxonomy:v1#/$defs/principalId"
    },
    "immediate_sender": {
      "description": "Principal sending this exact hop; never substitutes for the origin when deciding authority.",
      "$ref": "urn:japp:schema:security:capability-taxonomy:v1#/$defs/principalId"
    },
    "target_principal": {
      "description": "Final executing principal; must equal the command catalog's intended target.",
      "$ref": "urn:japp:schema:security:capability-taxonomy:v1#/$defs/principalId"
    },
    "authorization_profile": {
      "$ref": "urn:japp:schema:security:capability-taxonomy:v1#/$defs/authorizationProfileId"
    },
    "occurred_at": {
      "$ref": "urn:japp:schema:common:timestamp-utc:v1#/$defs/utcTimestamp"
    },
    "correlation_id": {
      "$ref": "urn:japp:schema:common:correlation:v1#/$defs/correlationId"
    },
    "causation_id": {
      "$ref": "urn:japp:schema:common:correlation:v1#/$defs/causationId"
    },
    "payload_size_bytes": {
      "description": "Exact encoded payload size metadata. The payload itself is not permitted in this record; authorization requires equality with the receiving transport's independently observed byte count.",
      "type": "integer",
      "minimum": 0,
      "maximum": 9007199254740991
    },
    "payload_digest": {
      "description": "Optional lowercase SHA-256 digest label for integrity correlation; never authorization authority.",
      "type": "string",
      "pattern": "^sha256:[0-9a-f]{64}$"
    },
    "idempotency_key": {
      "description": "Optional opaque key required only when the command catalog says so.",
      "$ref": "urn:japp:schema:common:stable-id:v1#/$defs/stableId"
    }
  }
}
