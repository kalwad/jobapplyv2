{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "urn:japp:schema:common:calendar-date:v1",
  "title": "Calendar dates without time",
  "description": "Civil calendar date (RFC 3339 full-date, YYYY-MM-DD) with no time component and no timezone. Calendar dates and timestamps are distinct types: a date never carries an implied midnight instant, and a timestamp is never truncated into a date by validation. Calendar validity (month lengths, leap years) is enforced by the date format assertion in addition to the pattern.",
  "x-japp-schema-version": "1.0.0",
  "$defs": {
    "calendarDate": {
      "title": "Calendar date",
      "type": "string",
      "format": "date",
      "pattern": "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"
    }
  }
}
