{
  "name": "@japp/evaluation-runner",
  "version": "0.0.1",
  "private": true,
  "description": "Deterministic evaluation-only benchmark runner, aggregation, regression comparison, and report projections for M02-W05. EVALUATION_ONLY and NON_PRODUCTION; never a product runtime dependency and never critical-gate authority.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run",
    "runner:check": "node src/cli.ts --check"
  },
  "dependencies": {
    "@japp/contracts": "workspace:*"
  },
  "devDependencies": {
    "@japp/evaluation-baselines": "workspace:*",
    "@japp/test-fixtures": "workspace:*",
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
