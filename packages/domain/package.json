{
  "name": "@japp/domain",
  "version": "0.0.1",
  "private": true,
  "description": "Shared pure domain logic (spec §5.1); populated as domain milestones (M06+) implement it. Scaffold created in M00-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
