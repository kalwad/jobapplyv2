{
  "name": "@japp/resume-schema",
  "version": "0.0.1",
  "private": true,
  "description": "Semantic resume document model and validators (spec §5.1); defined in M09-W01. Scaffold created in M00-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
