{
  "name": "@japp/ats-adapters",
  "version": "0.0.1",
  "private": true,
  "description": "ATS adapters — Greenhouse, Lever, Ashby, and later families (spec §5.1); first adapter arrives in M19. Scaffold created in M00-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
