/**
 * Redaction, validation, and capability checks (spec §5.1); populated as trust-boundary milestones implement it.
 *
 * Scaffold module created by M00-W02. Real implementations arrive with the
 * owning milestones; nothing here is product behavior.
 */
export const PACKAGE_NAME = "@japp/security";
