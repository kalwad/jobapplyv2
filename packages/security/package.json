{
  "name": "@japp/security",
  "version": "0.0.1",
  "private": true,
  "description": "Redaction, validation, and capability checks (spec §5.1); populated as trust-boundary milestones implement it. Scaffold created in M00-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
