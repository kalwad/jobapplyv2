{
  "name": "@japp/platform",
  "version": "0.0.1",
  "private": true,
  "description": "Typed OS capabilities, paths, process, key store, installer interfaces (spec §5.1, §5.14.2); typed platform contracts are owned by M01-W07 and native implementations by M03/M04/M05/M17/M27. Scaffold created in M00-W09.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
