{
  "name": "@japp/test-fixtures",
  "version": "0.0.1",
  "private": true,
  "description": "Synthetic documents, jobs, forms, and expected results (spec §5.1); seeded in M02 — never real PII (spec §1.5). Scaffold created in M00-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
