{
  "name": "@japp/test-fixtures",
  "version": "0.0.1",
  "private": true,
  "description": "Test-only synthetic profile, evidence, resume, job, expected-result, field-policy, question, and answer fixtures owned by M02-W01 and M02-W02; never product storage authority or real PII.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "fixtures:seed": "node scripts/generate-seed.ts",
    "fixtures:seed:check": "node scripts/generate-seed.ts --check",
    "fixtures:validate": "node src/cli.ts validate",
    "fixtures:privacy": "node src/cli.ts privacy",
    "fixtures:platform-v1": "node src/cli.ts platform-v1",
    "fixtures:discover": "node src/cli.ts discover",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run --no-file-parallelism --maxWorkers=1"
  },
  "dependencies": {
    "@japp/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "prettier": "3.9.6",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
