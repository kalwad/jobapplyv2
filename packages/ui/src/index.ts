/**
 * Shared UI components and accessibility helpers (spec §5.1); populated from the desktop shell milestone (M03) onward.
 *
 * Scaffold module created by M00-W02. Real implementations arrive with the
 * owning milestones; nothing here is product behavior.
 */
export const PACKAGE_NAME = "@japp/ui";
