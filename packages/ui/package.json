{
  "name": "@japp/ui",
  "version": "0.0.1",
  "private": true,
  "description": "Shared UI components and accessibility helpers (spec §5.1); populated from the desktop shell milestone (M03) onward. Scaffold created in M00-W02.",
  "type": "module",
  "scripts": {
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
