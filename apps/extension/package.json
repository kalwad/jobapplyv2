{
  "name": "@japp/extension",
  "version": "0.0.1",
  "private": true,
  "description": "Browser extension (WXT + TypeScript, Manifest V3 per spec §5.2). M02-W10 extends the verified W07-W09 feasibility substrate with canonical decision-authorized, control-specific transactional drivers, current semantic re-resolution, bounded settle verification, site acceptance, and undo on permitted loopback fixtures. It has no product UI, native host, database, live model runtime, navigation execution, final-form action, or external network authority.",
  "type": "module",
  "scripts": {
    "typecheck": "wxt prepare && tsc -p tsconfig.typecheck.json",
    "build": "wxt build -b chrome",
    "test": "vitest run --no-file-parallelism --maxWorkers=1"
  },
  "dependencies": {
    "@japp/contracts": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wxt": "0.20.27"
  }
}
